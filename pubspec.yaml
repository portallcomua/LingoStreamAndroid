name: lingostream
description: LingoStream AI - Smart Screen Translator
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Для HTTP запитів
  http: ^1.1.0
  
  # Для отримання версії додатку
  package_info_plus: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Додаємо іконку в ресурси
  assets:
    - assets/icon/
